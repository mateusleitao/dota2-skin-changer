[package]
name = "vpk-parser"
version = "0.1.0"
edition = "2021"
description = "VPK v1 file parser and KeyValues/VDF parser for Dota 2 items_game.txt"

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
